import { defineConfig } from 'dumi';

export default defineConfig({
  base: '/react-map-ol/',
  publicPath:'/react-map-ol/',
  outputPath: 'docs-dist',
  themeConfig: {
    name: 'react-map-ol',
  },
});
